import { ItemNameDirective } from './item-name.directive';

describe('ItemNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemNameDirective();
    expect(directive).toBeTruthy();
  });
});
