import { CourseIconPipe } from './course-icon.pipe';

describe('CourseIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseIconPipe();
    expect(pipe).toBeTruthy();
  });
});
